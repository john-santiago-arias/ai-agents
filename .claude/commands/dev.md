---
allowed-tools: Task, Read, Write, Edit, Bash, Glob, Grep
description: "Full-stack pipeline: analyst → architect + designer → apps-senior-frontend + backend → qa. Use for new features that need UI, API, and DB. Usage: /feature <requirement>"
---

Invoke @agent-pm with this requirement for a FULL-STACK pipeline:

$ARGUMENTS

The PM must classify this as `full-stack` and invoke all 7 agents.
