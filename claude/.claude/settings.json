{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "statusMessage": "Formatting (prettier) then fixing (eslint)...",
            "command": "f=$(jq -r '.tool_response.filePath // .tool_input.file_path // empty'); [ -n \"$f\" ] || exit 0; pnpm exec prettier --write --ignore-unknown \"$f\" 2>/dev/null; echo \"$f\" | grep -qE '\\.(ts|tsx|js|jsx|mjs|cjs)$' && pnpm exec eslint --fix \"$f\" 2>/dev/null; true"
          }
        ]
      }
    ]
  }
}
